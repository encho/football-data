(ns football-data.core-test
  (:require [clojure.test :refer :all]
            ;;[football-data.core :refer :all]
            ;;[football-data.football-data :refer [default-parser date-parser]]
            ))


;; (deftest a-test
;;   (testing "FIXME, I fail."
;;     (is (= 0 1))))


;; (deftest test-date-parser
;;   (testing "FIXME, I fail."
;;     (is (= 1 1))
;;     (let [
;;           date-string "30/01/01"
;;           parsed-date (football/date-parser date-string)
;;           expected
;;           ])

;;     )
;;   )




(run-tests)

;; ;; tests for default parser
;; (default-parser "")
;; (default-parser "aaa")
;; (default-parser "1.22")
;; (default-parser "1")


